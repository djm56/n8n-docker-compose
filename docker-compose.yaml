#version: "3.7"
services:
  n8n-server:
    image: n8nio/n8n
    container_name: n8n
    restart: unless-stopped
    volumes:
      - ./.n8n:/home/node/.n8n
      - ./local-files:/home/node/files
    ports:
      - "5678:5678"
    env_file:
      - ./.env